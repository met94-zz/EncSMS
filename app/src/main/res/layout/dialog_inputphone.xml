<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingBottom="8dp"
    android:paddingLeft="8dp"
    android:paddingRight="8dp"
    android:paddingTop="12dp"
    android:weightSum="1"
    android:baselineAligned="false">

    <Button
        android:id="@+id/pickContactButton_phoneDialog"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:text="@string/dialog_phone_pick"
        android:textAlignment="center" />

    <EditText
        android:id="@+id/phoneEditText_phoneDialog"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_below="@+id/pickContactButton_phoneDialog"
        android:hint="@string/dialog_phone_hint"
        android:inputType="phone"
        android:maxLines="1" />

</RelativeLayout>